# Batch 04: analysis and performance

All 10 exact queries q031–q040 were run against the frozen current index with local MiniLM and top 10 retrieval. Independent rg searches and source/test reads followed retrieval. Agent judgments: 9 good, 1 partial, 0 poor. This is a focused architecture-heavy historical slice, not a representative general-user score.

Strong successes: q032 puts the bounded TypeScript scanner test first and full traversal architecture second; q034 and q035 put check profiling/concurrent shared-context documentation first; q038 and q039 immediately find the collision-safe parser-cache specification and exact vN:SHA1 invalidation contract. q040 covers the compound source inventory/GUI watcher question across ranks 1, 3, and 5.

The partial case q037 finds nearest-project discovery in File Walking at rank 1, but symlink-root behavior is not explicitly documented. Source discovery uses statSync (following symlinks), whereas child traversal excludes symlinks; these are different behaviors. Most other returned sections concern explicit check directories or reference resolution. This is primarily a documentation coverage gap, not evidence that a better indexed answer was missed.

Minor ranking issues: q033 puts the actual extension list at rank 6 and central registry architecture at rank 7, behind short test specs, though ranks 1–2 already answer the registry and coverage intent. q038 returns the unrelated locate leading-hash test at rank 3. q040 drifts to external sidebar/source meanings at ranks 9–10. These suggest candidate lexical ambiguity and test/architecture ordering hypotheses; no scoring change is established by this small batch.

Raw search results and per-result grades are preserved in results/q031.json through q040.json and reviews/04.json. No repository, ranking, or shared-index changes were made.
