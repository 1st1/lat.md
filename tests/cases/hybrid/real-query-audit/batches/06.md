# Batch 06: navigation audit

Ran all ten historical queries (q051–q060) through the frozen current search snapshot and independently inspected documentation, source and tests using rg and file reads. This is an agent judgment audit, not human relevance labeling.

Nine queries are good at default top five. Each has a direct answer at rank one. Strong successes include H1 TOC scroll-top zero, source folding with a five-line buffer and preserved upward scroll, URL-backed search history, require-code-mention metadata, and ancestor validation markers. Same-document routing has direct answers first and second; rich-renderer preservation appears sixth but is not necessary for the core answer.

The Firefox heading reference-panel symptom (q051) is poor. Its first four results are CLI expansion/ref-resolution tests; the only useful top-five hit is source-reference context at fifth. The actual heading menu specification, Markdown navigation architecture and incremental refresh specification are all absent from the first ten. Source inspection finds SectionMenu local React state and stopped click propagation in MarkdownContent.tsx:184–245. No indexed Firefox-specific bug diagnosis exists, so this is a failure to retrieve relevant investigation context, not a proven missed root-cause answer. Browser-name/historical wording and overloaded refs/context terms plausibly contribute; no ranking fix was tested.

Ranking tails are noisy even for successful queries: clipboard, CLI expansion, search-preview expansion and generic validation compete with concrete navigation behavior. The first-answer performance is considerably better than whole-list precision in this batch. No production code or ranking changes were made.
