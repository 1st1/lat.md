# Rendering batch 07

All ten exact historical queries q061–q070 were run against the frozen current repository search snapshot (local MiniLM; top ten, default success judged at five). Each was independently checked with targeted ripgrep plus source/test/document reads. These are agent judgments.

- Seven good, one partial, two no-indexed-answer cases.
- Clipboard query retrieves core behavior, failure handling, exact text preservation, and implementation architecture at ranks 1–4. H1 TOC indentation and end-of-document activation each put both precise docs at ranks 1–2. Mobile overflow and static asset behavior also have direct default answers.
- Segmented wiki-link underline/color query is the clearest ranking miss: Markdown navigation, which states underlining, is rank 7; the exact muted-path/bound-language-badge specification is outside top ten. Rank 1 instead describes reference counts. CSS and tests directly establish the requested behavior.
- Language-icon first-word wrapping returns the exact spec at rank 5, but fenced-code highlighting ranks first. Git overlap gets exact answers at 1 and 3, surrounded by irrelevant hook threshold tests. These are visible lexical/semantic collisions even when default retrieval succeeds.
- TOC icon licensing is a source-only answer: DocumentToc.tsx:21 explicitly credits Tabler list-tree (MIT), while indexed docs discuss layout without attribution. This is an indexing coverage limit, not an existing-answer rank failure.
- Compact muted reference-definition rows appear to be historical desired behavior, with no corresponding current implementation/spec found. Current docs cover reference validation and standard renderer behavior, not those visual rows. Do not count it as an established ranking failure.

No production code, formulas, documents, or index were modified.
