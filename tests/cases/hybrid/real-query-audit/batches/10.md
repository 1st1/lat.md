# Development query audit (batch 10)

All 10 exact historical queries q091–q100 were run against the frozen local index using run-batch.mjs 10, then checked against independent ripgrep searches, documentation and source/tests. This is an agent judgment, not human-labeled ground truth.

Six good, three partial, one no indexed answer at the default top five.

- Strong: Dart grammar plus annotation/dangling-reference tests are ranks 1–2 (q092); Java contracts and tests are ranks 2–3 (q093); WASM glue/runtime asset contracts dominate q094; Windows compatibility test and POSIX invariant are ranks 1–2 (q095); native linker setup is rank 1 (q096).
- Ranking miss: q091 finds CI matrix first, but PR preview setup is ranks 6 and 8, behind terminal section previews (rank 2, lexical rank 1) and unrelated external previews. The exact preview answer exists and is retrieved below the default cutoff.
- Documentation defect, not ranking defect: q097 retrieves publication docs well, but those docs still claim four packages and omit stemmer from the publication order. Current workflow publishes five packages.
- Coverage gap: q098 returns release/versioning procedure, but there is no beta/dist-tag policy in indexed docs or explicit tag routing in workflow source.
- Coverage and mild ranking issue: q099 returns init second, behind MCP operation. The exact installation command exists in README and the headingless root lat.md preamble, neither represented as a section in the frozen section manifest.
- Source-only decision: q100 returns template navigation but no direct language-enumeration policy. Actual templates use generic supported-source wording plus examples; canonical extension registry holds the comprehensive list. Do not label an unrecorded design rationale as a missed ranked answer.

Potential follow-ups: evaluate compound-query facet coverage for CI/preview, preserve relevant headingless preambles in retrieval or add an indexed Install section, and repair stale package-count docs. These are hypotheses/findings; no ranking or repository edits were made by this reviewer.
