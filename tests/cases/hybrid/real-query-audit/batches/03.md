# External-source query audit (batch 03)

All 10 exact historical queries q021–q030 were executed against the frozen current index with local MiniLM and top 10 results. Grades use the default top 5. This is an agent judgment, not human ground truth.

Six queries are good and four partial; none lack an indexed answer. Individual result grades and command/source evidence are in `03.json`; full targeted grep output is in `03-grep.json`.

- Strong successes: cache lifecycle q022 places exact invalidation/layout answers at 1/2; multi-part cache/schema query q024 covers cache, template, and handle rules in top 5; URL-template inference q028 gives its exact specification at 1; supported formats q029 gives tests and parser rules at 1/2. Named fragments versus line-range drift q026 has the exact rationale at 1.
- Cross-feature confusion: q023 external extension policy places local repository-path permissiveness at 1–3 while precise external rules rank 8. The two features differ materially: local references permit unsupported/extensionless files; external references require supported or configured default extensions.
- Hierarchy/compound intent: q027 mixes prefix-scoped links and sparse checkout. Checkout topics surface, but the concrete prefix mapping ranks 9, with unrelated local repository-path answers filling ranks 4–7.
- Actionable diagnostics: q025 returns general error behavior and test inventory, while existing concrete fetch-template/HTML correction messages and remote mismatch details are outside top 10.
- Historical naming: q030 finds `lat external show` through agent/checkout documentation but omits both the direct command description and explicit statement that `get-source` was not implemented. Override semantics arrive at 6–7; the top 5 is only partial for the full request.
- Noise despite success: q021 returns the correct AsciiDoc alias details at 3, but ordinary Markdown GitHub-slug tests occupy 1 and 5. Their policies should not be applied to external AsciiDoc anchors.

The observed misses suggest testing whether query scope (external versus local paths) and compound-aspect coverage can improve reranking. These are hypotheses; this audit did not change formulas or establish a causal retrieval-stage failure.
