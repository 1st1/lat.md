# Deployment query audit (batch 08)

All 10 exact historical queries q071–q080 were run with run-batch.mjs 08 against the frozen local index, then independently checked using ripgrep and source/test reading. Agent judgments: 8 good, 2 partial, 0 poor at default top five.

Strong successes: q071 returns static payload hashing/bootstrap at ranks 1–2; q073 returns actual Vercel tracing architecture at rank 1 and detailed test contract at rank 3; q079 returns preindexed runtime and warm reuse at ranks 1–2; q080 returns source payload deduplication at rank 1, confirmed by a test proving multiple source views share one body.

Concrete misses: q076 (serverless search, ephemeral cache, no Git/watchers) does not return either Server export or Builds a portable server deployment within ten results. Those sections explicitly answer it. Instead the top five contain CLI search, deployment packaging, unrelated external cache, and live watcher architecture. Negation and compound intent are plausible contributors, not experimentally established causes. q074 answers the tracked-file scanning half but puts the complete overwrite/force/no-marker explanation at rank 6, just beyond the default window.

Distractors: q072 ranks canonical external-commit export above actual canonical homepage routing, although rank 2 answers it. q078 ranks init output-ignore and instruction-marker sections above the relevant force-replacement docs at ranks 3–4. Current no-marker policy is explicit, so the historical marker wording should not be treated as an unimplemented feature. q075 lacks deployment-protection guidance but answers cache policy; q077 answers Node portability but does not establish Cloudflare Workers compatibility. No ranking changes were made.
